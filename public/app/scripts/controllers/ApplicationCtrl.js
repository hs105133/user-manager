'use strict';

angular.module('userApp').controller('ApplicationCtrl', function($scope, $location, $rootScope) {
       // $scope.hidenav = false;	
       // $scope.$on("hideNavbar", function(){
       // 		$scope.hidenav = true;
       // });
});
